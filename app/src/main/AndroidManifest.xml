<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".ConsumeApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Consumable">

        <activity android:name=".mvvm.news.NewsDetailActivity"/>
        <activity android:name=".mvvm.pixabay.PixabayActivity" />
        <activity android:name=".mvvm.sport.SportActivity" />
        <activity android:name=".mvvm.movies.MoviesActivity" />
        <activity android:name=".mvvm.meal.MealActivity" />
        <activity android:name=".mvvm.news.NewsActivity" />

        <activity
            android:name=".mvvm.main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>